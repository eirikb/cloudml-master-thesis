\begin{figure}[t]
  \begin{center}
    \subfigure[Requirement dependencies.]{
      \begin{tikzpicture}[scale=0.8, transform shape]
        \node (foundation) [class] { 
          \textbf{\citereq{foundation} (\emph{Strong technological foundation})} 
        };
        \node (lexical-template) [class, below=of foundation] { 
          \textbf{\citereq{lexical-template} (\emph{Lexical template})} 
        };
        \node (software-reuse) [class, right=of foundation] { 
          \textbf{\citereq{software-reuse} (\emph{Software reuse})} 
        };
        \node (mda) [class, above=of software-reuse] { 
          \textbf{\citereq{mda} (\emph{Model-Driven approach})} 
        };
        \node (multicloud) [class, below=of software-reuse] { 
          \textbf{\citereq{muli-cloud} (\emph{Multi-cloud})} 
        };
        \node (m@rt) [class, above=of foundation] { 
          \textbf{\citereq{m@rt} (\emph{Models@run.time})} 
        };

        \draw [line] (foundation) -- (lexical-template);
        \draw [line] (foundation) -- (software-reuse);
        \draw [line] (foundation) -- (m@rt);
      \end{tikzpicture}
    }

    \subfigure[Legend.]{
      \begin{tikzpicture}[scale=0.7, transform shape]
        \node (Requirement) [class] { Requirement };

        \node (AuxNode01) [text width=3cm, right=of Requirement] {};
        \node (AuxNode02) [right=of AuxNode01] {};

        \draw[line] (AuxNode01) -- node[below] {Dependency, both ways} (AuxNode02);
      \end{tikzpicture}
    }
  \end{center}
  \caption{Requirements, including dependencies.}
  \label{fig:requirements}
\end{figure}
